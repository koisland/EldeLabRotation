channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # Python file parsing
  - python==3.12
  - polars
  # General sequence analysis tools
  - samtools
  - seqkit
  # Call variants
  - sniffles==2.8.0
  - hiphase==1.7.0
  # Liftover
  - ucsc-liftover
  - paf2chain
